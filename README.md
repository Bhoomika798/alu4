# alu4
An ALU4 (4-bit Arithmetic Logic Unit) is a digital circuit that performs arithmetic and logical operations on two 4-bit binary numbers. It can perform operations such as addition, subtraction, AND, OR, and XOR based on control signals.
